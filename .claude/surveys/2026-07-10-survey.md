# Survey — 2026-07-10 — scope: DEEP (strategic review + 7-day code audit)

Requested: full strategic review (gaps, improvements) + audit of every file touched
since 2026-07-03. All claims below measured this session at HEAD 13ad59e69 + working
tree; nothing asserted from memory without re-verification.

## Executive read

Sprint 3's engineering is the best code this repo has shipped — S1b/S2/S3/S7 are
documented, fail-closed, leak-proof-by-construction, and carry 38 tests (suite: 315
passed in 2.63s; zero prompt drift vs the 07-09 baseline; 07-09's CRITICAL F1 is
closed — all Sprint 3 artifacts tracked and pushed). The highest-leverage move is no
longer engineering: **the council's publication gate has a live defect instance on
main** — `dustin-martin-the-storm.md` was committed despite a recorded HELD decision
and a standing same-hash DataSentinel FAIL, because `check-council-stamp.sh` accepts
any matching PASS and ignores a co-existing FAIL at the same content hash. Second
move: the S3 lineup backfill (700 garbage rows; 100% of 2026) is now the oldest
open HIGH and grows weekly.

## Ranked findings

### F1 — Stamp gate accepts a same-hash FAIL; a HELD doc shipped through the hole  [severity: CRITICAL] [class: 4 — LLM-judgement-as-gate + gate defect]
- Evidence: `scripts/council-content-hash.sh docs/news/2026-06-21-dustin-martin-the-storm.md`
  → `16ddae68…`; audit records at that exact hash: PASS (20260709T050709Z) **and FAIL
  (20260709T051141Z — the later record)**. `docs/pending-decisions.md` footer (committed
  da89a31c7, 16:13 AEST): "dustin-martin + forgotten-heroes **HELD** … trigger a
  DataSentinel FAIL on the current content." The doc was then modified and committed in
  13ad59e69 (16:37) and is live on main at that same hash. Gate hole pre-documented in
  Gaffer memory `feedback_datasentinel_nondeterminism.md`: "`check-council-stamp.sh` …
  accepts any matching PASS and ignores a co-existing same-hash FAIL. That is a gate
  hole to fix." This survey confirms the hole has now passed real content.
- Impact: "never publish around a FAIL" is breached on the record; the deterministic
  gate's authority is undermined; the HELD entry in pending-decisions is now false.
- Owner: **Gaffer** — and per escalation rules this is a defective-gate finding:
  **escalate to the human immediately.** Trade-off in one sentence: making the stamp
  gate fail-closed on any same-hash FAIL will block ships until a FAIL-flagged doc is
  remediated and re-gated, which is the intended behaviour.
- Recommended outcome: (1) stamp gate fails when ANY sentinel record at the current
  content hash is non-PASS; (2) the human decides dustin-martin's disposition
  (remediate+re-gate, or record a dated waiver); (3) pending-decisions footer updated
  to the true state. I do not dispute either verdict — both stand; the defect is the
  gate that arbitrated between them.
- Effort: S · Impact-per-day rank: 1

### F2 — DataSentinel's untagged-number sweep is non-deterministic (root cause of F1's contradiction)  [severity: HIGH] [class: 4]
- Evidence: Gaffer memory `feedback_datasentinel_nondeterminism.md` (observed 07-09:
  forgotten-heroes PASS, PASS, then FAIL with 54 itemized untagged numbers; dustin-martin
  PASS-with-0 and same-hash FAIL coexisting). `docs/pending-decisions.md` footer names it
  an "open harness issue."
- Impact: same bytes, opposite verdict — every gated ship inherits a coin-flip on the
  untagged-number rule; remediation loops (incremental flag reveal) burn agent cycles.
- Owner: **Gaffer** (gate design; the deterministic replacement is Scientist-buildable).
- Recommended outcome: the untagged-number sweep becomes a deterministic script (regex
  number-shape scan minus [data]-tagged spans, allowlist for years/rounds/jerseys), run
  as part of the gate; DataSentinel's LLM pass retains only semantic checks. This is the
  documented class-4 pattern: replace LLM judgement with a re-measure.
- Effort: M · Impact-per-day rank: 2

### F3 — S3 lineup backfill still unrun: 700 garbage rows, 100% of 2026  [severity: HIGH] [class: 5] (carried, re-measured)
- Evidence: fingerprint scan re-executed this session → `garbage rows total: 700 |
  by year: {2025: 412, 2026: 288}`; rows on disk 2026 = 288 (all garbage). Recipe at
  `docs/experiment-log.md` (S3 entry); `refresh_and_rank.sh:101-102` still excludes
  lineups from auto-stage "until their scraper corruption is fixed (S3)."
- Impact: BriefBuilder/DataSentinel declared source contains jersey numbers for every
  2026 selected side; blocks S6; re-scrape window grows each round.
- Owner: **Scientist** (execute; human supplies network window). The recipe still has
  no named runner/date — the 07-09 anti-pattern warning is aging into permanence.
- Recommended outcome: rows deleted, re-scraped with the fixed parser, fingerprint
  re-run to zero, allowlist exclusion lifted (Gaffer's line) in the same cycle.
- Effort: S · Impact-per-day rank: 3

### F4 — TOG% is live in production predictions with zero backtest evidence; queue never recorded  [severity: HIGH] [class: 2] (carried F4, escalated)
- Evidence: S1b wired `percentage_of_game_played→percentage_time_played` at 13ad59e69;
  the next prediction run (R20, ~2026-07-14) trains on it. No backtest entry since;
  `grep -n "queue" docs/experiment-log.md` → no serialized queue recorded. New
  measurement this session (60-file sample): TOG% non-null **0.0% pre-2000, 73.0%
  2000s, 100% 2010s, 99.3% 2020s** — the 07-09 watch item is now quantified: the
  feature is a missing-data cliff across training eras.
- Impact: R20 ships on an unvalidated feature-set change; if S7's flag is ever flipped
  in the same run, S1b/S7 effects confound permanently.
- Owner: **Scientist**.
- Recommended outcome: before R20's cycle, either (a) run the S1b-effect incremental
  backtest and log it, or (b) record an explicit dated acceptance of shipping unmeasured.
  Then the serialized queue in the experiment log: S1b-effect → S7 ON/OFF → S4 → S5,
  each entry noting the TOG% era-coverage figures above.
- Effort: S (discipline) + backtest CPU hours · Impact-per-day rank: 4

### F5 — Un-gated remediation edit parked in the working tree on a gated doc  [severity: MEDIUM] [class: NEW — in-flight work parked past its gate]
- Evidence: `docs/hall-of-fame-forgotten-heroes.md` modified 2026-07-09 16:37:53
  (46 s after the Sprint 3 commit), +13 bare `**[data]**` tags on prose restatements
  (the routed Scientist tagging pass per pending-decisions). Its council hash
  `2bb69904…` has **no sentinel record**; HEAD's hash `2faa3c20…` has a PASS. Parked
  un-gated for >1 day.
- Impact: the tree diverges from the last gated state; the fail-closed hook will
  (correctly) block any commit sweeping it in, surprising the next committer; if the
  tree is reset, the tagging work is lost.
- Owner: **Scientist** (finish: re-gate the current content) then **Gaffer** (commit or
  revert — never leave a gated doc modified across days).
- Effort: S · Impact-per-day rank: 5

### F6 — pending-decisions footer is false on two counts  [severity: MEDIUM] [class: 1 — record/staleness drift]
- Evidence: footer says forgotten-heroes HELD (it shipped, properly gated, 16:33 PASS →
  16:37 commit) and dustin-martin HELD (it shipped un-cleanly — F1). Both "HELD" claims
  are now wrong in opposite directions.
- Impact: the decision record — the artifact agents consult before acting — misstates
  ship state; the 07-09 anti-pattern ("completion notes follow the commit") recurring
  in mirror image (hold notes outliving the ship).
- Owner: **Gaffer**.
- Recommended outcome: footer updated to actual state, referencing F1's human decision
  for dustin-martin.
- Effort: S · Impact-per-day rank: 6

### F7 — README quickstart instructs a command the harness now refuses  [severity: MEDIUM] [class: 7 — phase-coupling drift]
- Evidence: `README.md:55` — `bash refresh_and_rank.sh`; `refresh_and_rank.sh:16-18`
  now errors: "not an entry point … Run: bash scripts/weekly_refresh.sh". Also: last
  full harness log is `weekly_refresh_2026-06-22.log`, yet R18/R19 predictions were
  generated 06-29/07-07 — recent cycles ran phases directly; the F04 completion
  sentinel (`last_refresh_complete.json`) still does not exist.
- Impact: a new operator following README hits a refusal; the sanctioned entry point
  has not produced a log in 18 days, so the single-entry guard remains unexercised.
- Owner: **Gaffer**.
- Recommended outcome: README points at `scripts/weekly_refresh.sh`; the 07-14 cycle is
  run through it end-to-end (this doubles as the F04 WATCH test — sentinel must appear).
- Effort: S · Impact-per-day rank: 7

### F8 — S6 card still assumes a position source that does not exist  [severity: MEDIUM] (carried F3 07-09, unchanged)
- Evidence: `docs/pending-tasks.md:216` — "Depends on: S3 (repaired lineup data is the
  most plausible position source)"; lineup schema re-verified this session:
  `[year,date,round_num,team_name,players]`, no position field.
- Owner: **Scientist** (source spike) + **Gaffer** (re-scope card). Effort: S · Rank: 8

### F9 — Conceded stats: repaired, still orphaned  [severity: MEDIUM] (carried F6 07-09, unchanged)
- Evidence: `data/conceded_stats/` holds only the 2025 file; repo-wide "conceded"
  consumers = builder, its test, prose in `update_team_analysis.py`. No load-bearing/
  demote decision recorded.
- Owner: **Scientist** + **Gaffer**. Effort: S–M · Rank: 9

### F10 — afl-insights "latest brief" pointer stale at R9; latest brief is R14  [severity: MEDIUM] (carried F5 07-09; delta: latest is R14, not R13)
- Evidence: `docs/afl-insights.md:18` links the R9 executive summary as "The latest
  brief"; `ls -t docs/coaches-strategy-corner/` → four round-14 briefs newest.
- Owner: **Gaffer** (fold into F17). Effort: S · Rank: 10

### F11 — Model training core has no behavioural tests  [severity: MEDIUM] [class: NEW — test-coverage gap]
- Evidence: `tests/unit/` (5,246 lines) covers scrapers, gates, features, sync — but
  no test exercises `_engineer_features` rolling/expanding computation,
  `extract_round_number`, or train/predict round-tripping on a synthetic frame.
  Sprint 3 tests pin feature *declaration* (test_prediction_features.py) and the pure
  S7 functions, not the engineered values.
- Impact: the highest-blast-radius module (production predictions) is guarded only at
  its edges; a rolling-window off-by-one would ship silently.
- Owner: **Scientist**.
- Recommended outcome: a synthetic-fixture test module asserting known rolling-average
  values, cutoff behaviour, and feature-matrix stability (the S7 flag-OFF byte-identity
  check exists ad hoc in the experiment log — promote it to a test).
- Effort: M · Rank: 11

### F12 — Three stale worktrees ~560 MB  [severity: LOW] (carried F7 07-09, unchanged)
- Evidence: `git worktree list` → same three June-16 worktrees. Owner: **Gaffer**. Rank: 12

## Last-7-days code audit (2026-07-03 → 07-10)

Commits: e525b82c7, 4d289665b, da8dd4394 (07-07); 5a5789bd1, 0b07b3048, da89a31c7,
13ad59e69 (07-09). Suite at HEAD: **315 passed in 2.63 s**; HOF deterministic gate
exit 0; CI `tests.yml` runs pytest on push.

| File | Purpose | Quality verdict | Tests |
|---|---|---|---|
| `supercoach/prediction.py` | S1b TOG% rename + phantom `cba_percent` removal; S7 opt-in flag + `_add_age_experience_features` | **Good.** Comments cite tasks; flag defaults OFF; feature list construction explicit. Import fallback (`sys.path.insert`) is mildly hacky but contained. | 5 declaration-guard tests; core engineering untested (F11) |
| `scrapers/game_scraper.py` | S3: header-detected Player column, name reversal, `LineupParseError` fail-loud | **Very good.** Survives column reorder; footer/sub-marker filtering; raises instead of returning garbage — directly closes the documented failure mode. | 4 tests, HTML fixtures, no network — compliant |
| `scripts/feature_engineering.py` | S7 pure functions | **Very good.** Leak-proof invariant documented and tested (append-future-game guard); stable mergesort for tie determinism. | 12 tests incl. leakage guards |
| `scripts/build_conceded_stats.py` | S2 idempotent rotation repair | **Very good.** Refuses on rows matching neither invariant (fail-closed); post-condition assert; idempotent. | 11 tests (repair + live-file schema/invariants) |
| `data/conceded_stats/team_stats_conceded_2025.csv` | S2 regenerated data | Invariant `disposals=kicks+handballs` holds (verified 07-09, 105/105 rows) | test_conceded_stats gates it |
| `refresh_and_rank.sh` (e525b82c7) | D3 auto-stage of scraped ground truth | Good — explicit allowlist, never `git add .`; lineups exclusion documented with reason. README now contradicts its entry-point guard (F7) | n/a |
| `tests/unit/test_top100_sync.py` | F10 sync guard | Adequate — 6 structural checks; reads real repo files by design (sync test) | self |
| `.claude/agents/BriefBuilder.md` | F06 year de-hardcode | Correct one-liner | n/a |
| `docs/hall-of-fame-forgotten-heroes.md` | HOF fill-zero corrections + spec-form tags | HEAD gated (PASS 2faa3c20); working-tree remediation un-gated (F5) | HOF gate covers stat pages |
| 4 news docs + `docs/pending-decisions.md` | F02 corrections | 3 docs DataSentinel-clean; dustin-martin shipped around a same-hash FAIL (**F1**); footer stale (**F6**) | stamp gate (defective, F1) |
| `docs/experiment-log.md` | S1b/S3/S7 records | Exemplary experiment discipline; "committed" claims now true post-13ad59e69 | n/a |
| `data/lineups/*`, `data/matches/matches_2026.csv`, player_data (4d289665b) | R18 ground truth | Committed on cadence; 2026 lineup content is pre-backfill garbage (F3) | fingerprint scan |

## What can be done better (beyond ranked findings)

- `supercoach/prediction.py:17-24` — try/except import with `sys.path.insert` at module
  top; prefer a package-relative import or a single entry-point that owns sys.path.
- Two prediction CSVs per round (R17–R19, pairs 30–200 min apart) — undiagnosed
  double-run habit; time the phases once and either fix the flake or record the reason.
- 71 `sentinel-*.json` and growing, no retention rule (watch, 07-09).
- `scripts/update_hof_pages.py:115` TODO (career kicks/handballs leaders) — the only
  code TODO in the repo; fine, but give it a task ID or delete it.

## Sprint 4 recommendations (sequenced)

1. **F1 first, human in the loop:** fail-closed same-hash-FAIL fix to the stamp gate +
   dustin-martin disposition + pending-decisions footer (F6). Half a day, restores gate
   authority before anything else ships through it.
2. **Close the parked HOF remediation (F5)** — re-gate and commit, or revert. One agent-hour.
3. **S3 backfill (F3)** in the next human network window; lift the allowlist exclusion.
4. **Backtest queue (F4):** S1b-effect run logged before/with the R20 cycle; then S7
   ON/OFF; then S4→S5. One change per comparison.
5. **Deterministic untagged-number sweep (F2)** — M-effort, removes the recurring
   verdict coin-flip; schedule as the sprint's one harness build.
6. **Cheap parallels:** README entry point (F7), S6 re-scope spike (F8), conceded-stats
   decision (F9), F17+insights pointer (F10), worktrees (F12).
7. **Run 07-14 through `weekly_refresh.sh` end-to-end** — F04 sentinel WATCH falls due.

## Anti-pattern list (standing)

All 16 items from 2026-07-09 retained (see that survey). Added this survey:
- **A same-hash PASS does not clear a same-hash FAIL** — ship criterion is *zero
  non-PASS records at the current content hash*, not *any PASS exists*. (F1)
- **Never park a modified gated doc across days** — finish the remediate→re-gate→commit
  loop in one session or revert. (F5)
- **A "HELD" note is a live lock, not prose** — releasing a hold requires updating the
  record in the same commit that ships the doc. (F6)

## Watch list (speculation, unranked)

- F04 first live test now due at the 07-14 cycle (log + completion sentinel must both appear).
- TOG% era cliff → RESOLVED into F4 with measurements (0%/73%/100%/99.3% by era).
- Prediction double-runs — carried.
- Sentinel record retention — carried (71 files).
- `weekly-fan-pack.yml` (Wed 23:00 UTC) packages HEAD; if a cycle slips past Thursday
  09:00 AEST, fans get last week's pack silently — worth one look at ordering vs the
  Tuesday refresh.

## What was checked and found clean

- Suite: 315 passed in 2.63 s at HEAD; CI tests.yml runs pytest on push.
- `check_hof_numbers.py`: all verified OK, exit 0.
- 07-09 F1 (Sprint 3 uncommitted) **closed**: all 8 artifacts tracked (`git ls-files`
  verified), main == origin/main, experiment-log "committed" claims now true.
- Prompt drift: all 11 agent/skill hashes identical to the 07-09 baseline — zero drift.
- forgotten-heroes at HEAD: council hash 2faa3c20 = PASS record, no same-hash FAIL.
- Cadence: R18 settled in `matches_2026.csv`; R19 predictions generated 07-07; insights
  Week-in-Review at R19; README news block within the 2-entry limit (not re-verified
  this session — enforced by harness function).
- Hooks: fail-closed stamp verification logic read end-to-end (`check-council-stamp.sh`
  + `council-content-hash.sh`) — sound except the same-hash-FAIL hole (F1).
- No `index.lock` residue; only one code TODO repo-wide.
- S1b/S2/S3/S7 diffs read line-by-line — no correctness issues found beyond F11's
  coverage gap.

Baseline: prompt-hash baseline unchanged and still valid (BriefBuilder's F06 hash now
committed as predicted by the 07-09 baseline note).
