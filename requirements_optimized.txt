# Optimized requirements with version pinning for better performance and security
# Core data processing (optimized versions)
pandas==2.1.4
numpy==1.24.4

# Machine learning (lightweight)
scikit-learn==1.3.2
joblib==1.3.2

# Web scraping (async optimized)
aiohttp==3.9.1
beautifulsoup4==4.12.2
requests==2.31.0

# Visualization (optional - can be removed for production)
matplotlib==3.8.2

# Progress tracking
tqdm==4.66.1

# Hyperparameter optimization (optional - reduced dependency)
optuna==3.4.0

# GPU support (optional - only if needed)
# lightgbm==4.1.0  # Uncomment only if GPU training is required

# Development dependencies (remove in production)
# datetime  # Built-in module, not needed
# bs4       # Alias for beautifulsoup4, removed duplicate